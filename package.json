{
  "name": "classical-text-reader",
  "version": "1.0.0",
  "private": true,
  "description": "Local, sentence-by-sentence classical Chinese reader and notebook.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
