document.addEventListener('DOMContentLoaded',async()=>{const c=document.querySelector('#works');try{const {works}=await Reader.getJson('/api/works');c.innerHTML=works.length?works.map(w=>`<a class="index-card" href="/work.html?id=${encodeURIComponent(w.id)}"><div><h2>${Reader.escapeHtml(w.title)}</h2><p>${Reader.escapeHtml(w.author||'传统归属未注明')}</p></div><dl><div><dt>已收录章节</dt><dd>${w.chapterCount}</dd></div><div><dt>已完成章节</dt><dd>${w.completedChapterCount}</dd></div><div><dt>最近阅读章节</dt><dd>${w.recentChapter?`第${w.recentChapter.number}章 ${Reader.escapeHtml(w.recentChapter.title)}`:'尚未阅读'}</dd></div><div><dt>最近修改</dt><dd>${Reader.formatTime(w.updatedAt)}</dd></div></dl></a>`).join(''):'<p class="muted">尚未收录作品。</p>';}catch(e){console.error(e);Reader.showError(c,e.message);}});
